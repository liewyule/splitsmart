export const metadata = {
  title: "SplitSmart - Login",
  description: "Log in to SplitSmart"
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
